#ifndef CUBICO_OPTIMIZADO_H
#define CUBICO_OPTIMIZADO_H

#include <vector> // Incluir la biblioteca para std::vector

// Usar el espacio de nombres std
std::vector<std::vector<int>> mulOp(const std::vector<std::vector<int>>& matrix_A, const std::vector<std::vector<int>>& matrix_B);

#endif // CUBICO_OPTIMIZADO_H
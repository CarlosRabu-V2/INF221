#ifndef CUBICO_TRADICIONAL_H
#define CUBICO_TRADICIONAL_H

#include <vector> // Incluir la biblioteca para std::vector

// Usar el espacio de nombres std
std::vector<std::vector<int>> mulMat(const std::vector<std::vector<int>>& matrix_A, const std::vector<std::vector<int>>& matrix_B);

#endif // CUBICO_TRADICIONAL_H